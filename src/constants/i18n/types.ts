/**
 * Shared types for the i18n dictionary split.
 * `Locale` is exported from a tiny file so every dictionary module can
 * import it without circular references.
 */
export type Locale = 'en' | 'bn';
